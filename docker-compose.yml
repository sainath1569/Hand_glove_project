version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./web_interface/uploads:/app/web_interface/uploads
      - ./web_interface/results:/app/web_interface/results
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
